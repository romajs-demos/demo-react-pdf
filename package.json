{
  "name": "demo-react-pdf",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "querystring": "^0.2.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-pdf": "^4.0.5",
    "react-scripts": "3.0.1",
    "webpack-log": "^2.0.0"
  },
  "scripts": {
    "deploy": "now -n demo-react-pdf -Q build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "now": "^15.3.0"
  }
}
